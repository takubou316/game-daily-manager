# CLAUDE.md

このファイルはClaude Code（claude.ai/code）がこの`game-daily-manager`フォルダで作業する際のガイドです。

## アプリ概要（2026-09-06に方針拡張）

元々はCodexが作った「複数ゲームの日課・週課・期間限定タスクを一元管理するPWA」（React + Vite + Supabase）。
2026-09-06、`training-menu`（筋トレメニュー自動生成＋記録PWA、別リポジトリ）と接続し、両方を横断して
今日やることを一括確認できる**全体管理画面**をこのリポジトリ内に追加する方針を決定した。

技術スタック・データ設計思想の詳細は元々の[PLAN.md](PLAN.md)を参照（初期版のゲームタスク管理部分は
方針変更なし、そのまま有効）。このCLAUDE.mdでは、統合後の全体像と今後の長期方針を記録する。

## HANDOFF.mdについて

このCLAUDE.mdは長期の設計方針・注意点を置く場所。**今どこまで進んでいて次に何をするか**は、同じ
ディレクトリの[HANDOFF.md](HANDOFF.md)にまとめる。統合作業のフェーズ分けした計画は
[INTEGRATION_ROADMAP.md](INTEGRATION_ROADMAP.md)を参照。コード・設定に変更を加えたら作業終了時に
必ずHANDOFF.mdを更新する。

## 運用主体（2026-09-06時点）

- 元々は「ゲームタスク管理部分はCodex管轄、筋トレ部分はClaude管轄」で分担していたが、Codexが
  近く使えなくなる見込みのため、**この統合作業を含め、今後しばらくは全面的にClaude管轄**で進める。
- 作業日記はClaude側（Obsidian Vault「作業日記」）に記録する。

## アーキテクチャ：3層構成

筋トレとの統合にあたり、「ゲームタスク管理の中に筋トレを1タスクとして混ぜる」案は**採用しなかった**
（筋トレがゲームタスク管理に従属する構造になり、「ゲームタスク管理のさらに上に全体を見渡す画面が
欲しい」という要望とズレるため）。代わりに以下の3層構成にする。

```
全体管理画面（新規トップ、このリポジトリに追加）
  └─ 「今日のタスク」一覧
       - 初期表示: 今日期限のゲームタスクのみ + 筋トレの「今日やったか」を1項目としてUI層でマージ
       - 「すべて見る」切り替えで、未完了タスク全部（今のダッシュボード相当）にも展開できる
       - ゲームタスクの完了・+1操作はその場ですぐできる（既存のTaskRowをそのまま流用）
       - 筋トレ項目はtraining-menuへの遷移リンク（「今日済/未」の表示のみ、操作はtraining-menu側で行う）

ゲームタスク管理（既存のダッシュボード。tasks一覧・ゲーム管理・リソース管理。1階層下のルートに温存）
筋トレアプリ（training-menu、別リポジトリの独立アプリのまま。埋め込みはしない）
```

全体管理画面は「今日のタスク」を横断的に見るための新しいトップ画面であり、既存のゲームタスク
管理画面（tasks一覧・ゲーム管理・リソース管理）は変更せずそのまま1階層下に残す。

## データ設計：既存テーブルは無変更、筋トレ用に新規3テーブル

`games`/`tasks`/`task_periods`/`task_completions`/`resources`（[supabase/schema.sql](supabase/schema.sql)）
は無変更。筋トレの記録データは別テーブル群として正規化し、`games`/`tasks`への偽装登録はしない
（全体管理画面の「今日やったか」判定は`training_sessions`を直接見て行う）。

- `training_sessions`: 1回のトレーニング（日付・目的・所要時間・体重）
- `training_session_exercises`: セッション内の種目（種目ID・名前・並び順）
- `training_session_sets`: セットごとの重量・回数・RPE・ウォームアップ有無

いずれも既存テーブルと同じRLS方式（`auth.uid() = user_id`）で保護する。加えて2026-09-07、Codexに
設計レビューを依頼した際、「子テーブルが`user_id`単独のRLSだけだと、他人の`session_id`等を親として
指定した紐付けを防げない」との指摘を受け、子テーブルは**(親id, user_id)の複合外部キー**で親子が
同一ユーザーであることをDB制約として保証するよう修正した。また、親の`training_sessions`だけでなく
**各階層に端末発行の`local_id`を持たせ、親スコープでユニーク制約**を付けることで、オフラインキューが
同じ記録・同じ種目・同じセットを再送しても重複登録されないようにした（当初案は親にしか`local_id`が
無く、子の重複防止が抜けていた）。マイグレーションは
[supabase/migrations/20260906_add_training_integration.sql](supabase/migrations/20260906_add_training_integration.sql)。

## training-menu側の方針：ローカルが常に正、クラウドはオプトインの複製

training-menu（別リポジトリ）は「ビルドステップなし・依存ライブラリなし」という既存方針を維持する。
Supabase JS SDKはCDN経由で`<script>`1行追加するだけにし、npm/Viteの導入はしない。

- 初回起動時、「Googleでログインしてクラウド同期する」／「ログインせずこの端末だけで使う」を選択
- ログインしない場合: 今のtraining-menuと完全に同じ挙動（localStorageのみ、変更ゼロ）
- ログインした場合: 記録はまずローカルに保存（今と同じ、即座にUI反映）→ オフラインキュー経由で
  Supabaseへ複製。オフライン時はキューに溜め、次にオンラインになったタイミングで追いつく
  （iOSのPWAはBackground Sync APIがほぼ使えないため、「次に開いた時に追いつく」割り切りでよい）
- 過去の記録データの一括移行はしない。ログイン後に新しく記録した分だけがクラウドへ複製される対象
- 設定画面からいつでもログイン/ログアウトを切り替え可能
- Supabase障害時も、ローカルには正のデータが残り続けるため機能は落ちない（切り戻し不要）

### 認証は別オリジンで共有しない（2026-09-07決定）

`game-daily-manager`と`training-menu`は別々のGitHub Pagesサイト（別オリジン）で公開するため、
片方でGoogleログインしてももう片方には自動的に引き継がれない。これに対し「トークンを使った
自動ログイン」「同一オリジン化（デプロイだけ統合）」も検討したが、いずれも運用の複雑さ・
壊れやすさが増す代償が大きいと判断し、**割り切って別々にログインする**方針にした。
training-menu側でも初回だけ改めてGoogleログインが必要になるが、以後はブラウザにセッションが
残るため毎回ではない。「シームレスな移行」の本体（データが繋がっている、ダッシュボードから
状況が見える）はこれが無くても達成できるため、影響は軽微と判断した。

**運用メモ（2026-09-07判明）**: ローカル開発サーバー（`npm run dev`、デフォルトポート5173）で
Googleログインをテストするには、Supabase Auth設定の「Redirect URLs」に`http://localhost:5173/**`を
事前に追加しておく必要がある。登録されていないURLへの`redirectTo`は無視され、Site URL（本番URL）へ
リダイレクトされてしまい、ローカル環境での検証ができない。ポートを変える・新しい開発環境を作る際は
毎回このリストへの追加が要る。

## 全体管理画面の筋トレ欄：training_sessionsを直接クエリ（フェーズ5、2026-09-07）

`OverviewScreen`の筋トレ欄は、`training-menu`との認証非共有を前提に、`training_sessions`テーブルを
`user_id`（＝game-daily-manager側のログインユーザーのSupabase `auth.uid()`）で直接クエリして
「今日やったか」を判定する。3状態:

- `done`: 直近のセッションの`session_date`が今日
- `not_done`: セッションはあるが今日ではない
- `unknown`: セッションが1件も無い（training-menu側でまだクラウド同期を使ったことがない可能性が
  高い）、またはクエリ自体が失敗した場合。「未実施」と「クラウド未接続で分からない」を混同しない
  よう、`not_done`とは別の文言で表示する（Codexレビュー指摘への対応）

「今日」の判定に使う`todayDateKey`は、training-menu側の`js/storage.js`の`localDateKey`と全く同じ
ロジック（ブラウザのローカルタイムゾーンで`getFullYear/getMonth/getDate`）であることを実装を
突き合わせて確認済み。両アプリを同じ端末・同じタイムゾーンのブラウザで使う前提が崩れない限り
日付判定はずれない。

## 筋トレショートカット（2026-09-08〜）

「外の堤防を一周歩く」のように自由な名前のタスクをユーザーが登録し、特定の有酸素種目
（`CARDIO_EXERCISE_OPTIONS`、training-menu側の`type: 'cardio'`種目のid/nameを手動で複製した
もの）に紐づけると、ワンタップでtraining-menuの該当種目の記録画面（`?quickstart=<exerciseId>`、
training-menu側の`maybeHandleEntryParams()`が受ける）へ直接遷移できる機能。

- **定義と実績を分離する設計**（`games`/`tasks`と同じ考え方）。新規テーブル`training_shortcuts`は
  ショートカットの「定義」（ラベル・紐づけ種目）だけを持ち、「達成したか」は保存しない。全体管理
  画面を開くたびに、既存の`training_session_exercises`をその日の`exercise_id`単位で都度クエリし
  （`todayCompletedExerciseIds`）、ショートカットの`exercise_id`と一致すれば「達成」と判定する。
  `exercise_id`での厳密な一致のみで判定するため、別の種目を記録しても無関係なショートカットが
  誤って達成扱いになることはない
- `link_type`カラムは将来「種目に紐づかないショートカット」（例: 自分で作る画面への単純なリンク）
  にも対応できるよう汎用的な名前にしているが、現状は`'exercise'`のみサポート
- 達成済みショートカットをタップした時は、確認用に`trainingRecordUrl()`
  （training-menu側の`?view=record`）へ誘導する。未達成時と同じquickstart URLのままだと、
  確認したいだけなのに毎回新しい記録セッションが始まってしまうため分けている
- **`todayCompletedExerciseIds`は3値**: `Set`（成功、達成済み`exercise_id`の集合）／`null`
  （クエリ失敗等で「確認できない」）／非クラウドモードは常に空`Set`（達成という概念自体が
  無いため確定で「未実施」扱い）。`trainingStatus`（全体管理画面の筋トレ欄、フェーズ5）の
  done/not_done/unknownと同じ考え方で、一時的な通信エラーで全ショートカットが「未実施」と
  誤表示されないようにしている
- **タブ復帰時の自動再取得**: training-menuは別タブ/別ウィンドウで開く(`target="_blank"`)ため、
  記録・削除して戻ってきてもReactのstateは自動更新されない。`trainingRefreshToken`
  （`focus`/`visibilitychange`イベントでインクリメント）を`trainingStatus`/`trainingShortcuts`/
  `todayCompletedExerciseIds`の3つのuseEffectの依存配列に加えており、全体管理画面のタブへ
  戻ってきた時点で自動的に再取得される。
  **既知の罠（2026-09-08、実機でのみ発覚）**: `visibilitychange`/`focus`だけでは実機
  (iPhone Safari)で更新されず、ハードリロードしないと反映されない不具合が報告された。
  モバイルSafariのタブ切替は必ずしもこれらのイベントを発火させるとは限らず、また「戻る」操作が
  bfcache（ページをJS実行状態ごと凍結保存し、再訪問時にそのまま復元する仕組み）からの復元だと
  これらのイベント自体が発火しない。bfcache復元を検知できる`pageshow`イベントを追加し、
  さらにイベント方式が全滅しても最悪1分以内には追いつくよう、3つのuseEffectの依存配列に
  （60秒ごとに更新される）`now`も加えてポーリング的な保険を掛けている（二重の対策）。
- **フォーム保存の失敗時はフォームを閉じない**: `addTrainingShortcut`/`updateTrainingShortcut`/
  `deleteTrainingShortcut`はSupabaseエラー時に`showSyncError(error)`を呼ぶのではなく`throw`する
  規約にしている（`submitTaskForm`と同じ規約）。呼び出し元(`submitShortcutForm`等)がtry/catchで
  受け、成功した時だけ`closeShortcutForm()`する。以前は失敗時も無条件でフォームを閉じており、
  入力内容が失われる不具合があった（2026-09-08、設計レビューで発見）

設計レビュー（`codex-implementer`経由、diffベース）で発見・修正した項目は上記に反映済み。
その他、指摘はされたが対応を見送った項目は[training-menu/CLAUDE.md](../training-menu/CLAUDE.md)の
「既知の制約」節（複数タブでの同期競合）を参照。

## 機微データについて

体重・トレーニング内容はゲームの日課データよりやや機微性が高いが、他人の秘密情報を預かる
password-vaultとは性質が異なる（自分専用アプリが自分のSupabaseプロジェクトに自分のデータを置くだけ）。
既存の`games`等と同じRLSのみで対応する方針とし、追加の暗号化は行わない。

## 容量見積もり

training-menuの1セッションは種目の説明文まで含めるとlocalStorage上では10〜20KB程度だが、Supabase側は
正規化するためさらに小さくなる見込み。無料枠500MBに対し、週4回×10年でも数十MB程度で全く問題にならない。
懸念点は容量よりも「無料プロジェクトは1週間操作が無いと自動停止」の方だが、ゲームタスクを毎日触る
前提の同一Supabaseプロジェクトに相乗りさせることで実質解消している（新規Supabaseプロジェクトは作らない）。

## 実装ロードマップ

[INTEGRATION_ROADMAP.md](INTEGRATION_ROADMAP.md)にフェーズ0〜6の実装順序と検証基準をまとめている。
フェーズが終わるたびに動作確認→報告→次のフェーズ、の順で進める。

## 設計レビューの経緯（2026-09-07）

実装開始前にCodexへ設計レビューを依頼した（`codex-implementer`経由、ファイル編集はさせずレビューのみ）。
指摘を受けて「データ設計」節の複合外部キー・冪等性の修正、「認証は別オリジンで共有しない」の決定を
反映済み。その他の指摘（数値の範囲チェック、`create policy`の冪等化）もマイグレーションSQLに反映済み。
ロードマップ側の検証基準の不足（RLSの他ユーザーテスト、複数端末・トークン期限切れ等）は
[INTEGRATION_ROADMAP.md](INTEGRATION_ROADMAP.md)のフェーズ1・フェーズ4に追記済み。
