# ゲーム日課管理アプリ 計画書

作成日: 2026-08-14
作業主体: Codex（将来Claudeへ引き継げる構成にする）
状態: 初期版公開確認済み（PC・スマホ実機表示、本番Googleログイン、Supabase読み込み・永続保存を確認）

## 1. 目的

原神、NTE、崩壊：スターレイル、ドラクエウォーク、ハートピアスローライフなど、複数ゲームの日課・週課・隔週課・月課・期間限定タスクを一元管理する。

アプリを開いたときに、未完了で期限が近いものがすぐ分かり、少ない操作で完了や回数を記録できることを最優先とする。

## 2. 初期版の前提

- PCとスマホの両方で使えるレスポンシブWebアプリ／PWAにする。
- GitHubでコードを管理し、GitHub Pagesで公開する。
- Supabase Free Planでログイン、タスク定義、完了履歴を保存する。
- まずは自分一人用。複数人共有、フレンド機能、公開機能は対象外。
- 通知は初期版では作らない。
- 更新時刻は画面に表示しない。初期版は日本時間の日付・週・月を基準にする。次回期限の日数は周期から自動計算し、ユーザー入力にはしない。
- ゲームごとの実際の更新時刻に依存する拡張余地は残す。
- 2週間ごとのタスクは基準日から14日単位で期限を計算する。
- 期間限定タスクは開始日・終了日を持ち、終了日までの残り日数で期限を計算する。

## 3. 対象ゲーム

- 原神
- NTE（Neverness to Everness）
- 崩壊：スターレイル
- ドラクエウォーク
- ハートピアスローライフ

ゲームは後から追加・休止・再開・削除できるようにする。削除時は過去履歴を壊さない。
休止中のゲームはダッシュボードとゲームフィルターから隠す。
タスク登録時に新しいゲーム名が入力された場合、ゲーム一覧と絞り込みへ追加する。
タスクの「無効化」は履歴を残したまま一覧から隠す。「削除」は確認後にタスク定義と実績を削除する。
全タスクを一か所で検索・選択し、まとめて無効化・削除できる管理画面を用意する。ゲーム情報はタスク削除では消さない。
固定の試作値を表示する進捗カードは残さず、対象タスクがある場合だけ実データから表示する。

## 4. タスク種別

### 4.1 単発チェック型

一度実行すれば、その期間のタスクが完了する。

例: 毎日のログインボーナスを受け取る。

### 4.2 回数目標型

期間内に指定回数を達成する。1日にまとめて実行しても、複数日に分けてもよい。

例: スターレイルの歴戦余韻を週3回行う。

表示例: `今週 2 / 3回`、`残り1回`。

### 4.3 間隔型

前回の実行記録を基準に、指定日数後に再び対象となる。

例: 原神の素材・釣りなど、48時間周期の要素。

### 4.4 期間限定型

開始日と終了日を持つ。終了が近い未完了タスクを優先表示する。

例: ゲーム内イベントの報酬回収。

## 5. 周期

- 毎日
- 毎週
- 2週間ごと
- 毎月
- 指定曜日
- 期間内の回数目標
- 前回実行から指定日数後

期間をまたいだ未達成タスクは翌期間に持ち越さない。未達成履歴を残し、新しい期間は新しい進捗で開始する。

## 6. 優先順位と表示

未完了タスクを、次の情報から自動的に並べ替える。

1. 期限切れ・当日期限
2. 期限までの残り日数
3. 回数型の残り回数と残り日数の比率
4. 手動重要度
5. 所要時間

重要度は「必須」「できれば」「余裕があれば」の3段階とする。

トップ画面は次の順で構成する。

1. 今日やること
2. 期限が近いもの
3. 今週の進捗
4. 今月の進捗
5. 期間限定イベント

完了済みは初期状態で折りたたみ、誤操作時に取り消せるようにする。

## 7. 初期画面

### ダッシュボード

- 今日の日付
- 未完了タスク一覧
- ゲーム名、タスク名、期限、残り日数、重要度、設定されている場合のみ所要時間
- 単発型の完了ボタン
- 回数型の`+1`ボタンと進捗表示
- 完了済み一覧の開閉
- ゲーム別フィルター

### タスク管理

- タスクの追加、編集、無効化、削除
- ゲームの選択（登録済みゲームの幅広い候補検索＋クリック選択＋自由入力）
- タスク種別・周期・回数目標・重要度・所要時間・メモの設定
- 初回登録は簡単にし、詳細設定は折りたたむ

### ゲーム管理

- ゲームの追加、名前変更、休止、再開
- 休止中のゲームはダッシュボードから非表示
- ゲーム削除時に履歴を残すか確認

### 履歴・設定

- 過去の達成・未達成履歴
- JSONエクスポート／インポート
- Supabaseからの再読み込み
- ログアウト

## 8. データ設計の方針

最低限、次の概念を分離する。

- `games`: ゲーム定義
- `tasks`: 繰り返しタスクの定義
- `task_periods`: 期間ごとの進捗・達成状態
- `task_completions`: 1回ごとの実行履歴
- `profiles`: ユーザー設定

タスク定義と実績履歴を分離し、周期の変更やタスク名の変更で過去の履歴が壊れないようにする。

ユーザーIDを全データに紐づけ、Supabase Row Level Securityで自分のデータだけを読めるようにする。

## 9. 保存・同期・バックアップ

- 通常時はSupabaseを正本とする。
- UI操作後は画面へ即時反映し、保存失敗時はエラーを表示して再試行できるようにする。
- 初期版では完全オフライン編集は必須にしない。
- 将来、ローカルキャッシュとオフライン同期を追加できる設計にする。
- JSONエクスポートを初期版に含め、Supabaseの無料プラン停止やサービス変更に備える。

## 10. 実装順

1. プロジェクト雛形、PWA、レスポンシブレイアウト
2. ダミーデータでダッシュボードのUIを作る
3. 単発チェック型の完了記録
4. 回数目標型の進捗記録
5. 周期計算・期限計算・自動並び替え
6. ゲーム・タスクの追加編集
7. Supabase認証とデータ保存
8. 履歴、JSONエクスポート／インポート
9. 5ゲームの初期タスク登録
10. PC・スマホでの検証とUI改善
11. GitHub Pages公開

## 11. 検証基準

- 初回起動時に、説明なしで今日のタスクを確認できる。
- 単発タスクを1タップで完了・取り消しできる。
- 回数型タスクを同日複数回、または複数日に分けて正しく記録できる。
- 期限が近い未完了タスクほど上に表示される。
- 期間が変わると進捗が正しく切り替わり、未達成履歴が残る。
- PC幅とスマホ幅で、主要操作が迷わず押せる。
- 空状態、読み込み中、通信エラー、入力エラーを確認できる。
- 実機スマホで操作確認してから初期版完成とする。

## 12. 将来Claudeへ引き継ぐためのルール

- 仕様決定、実装変更、検証結果、未解決事項をCodex作業日記へ都度追記する。
- コード変更時は`HANDOFF.md`を更新する。
- 重要な設計判断はこの`PLAN.md`かプロジェクトの`CLAUDE.md`に反映する。
- Gitのコミット・プッシュは、明示的に依頼されない限りCodexでは行わない。
- Supabaseのテーブル定義・RLS・環境変数名を文書化する。秘密情報は記録しない。

## 13. 現時点の未決定事項

- フロントエンド技術（React + Vite等）の最終決定
- Supabaseのログイン方法
- GitHubリポジトリ名と公開URL
- 初期タスクの具体的な一覧
- ゲームごとの日付・週の基準を将来どこまで細かく設定できるようにするか

## 14. Supabase接続の進め方

- `@supabase/supabase-js`でブラウザからSupabaseへ接続する。
- `supabase/schema.sql`を基に、ゲーム、タスク、期間別進捗、完了履歴を保存する。
- Supabase AuthとRLSで自分のデータだけを扱う。
- ローカルで保存・読み込みを確認してからGitHub Pagesへ公開する。

## 15. 現在の実装確認

- Google OAuthでローカルアプリへログインできることを確認済み。
- 初回ログイン時にSupabaseから試作ゲーム・タスクが読み込まれることを確認済み。
- 回数型の進捗は本番URLで再読み込み後も残ることを確認済み。単発完了、タスク追加・編集は公開版で追加確認する。
