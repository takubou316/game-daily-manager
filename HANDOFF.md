# HANDOFF

更新主体: Codex
更新日時: 2026-08-14
状態: 初期版公開確認済み。GitHub Pages、Google OAuth、Supabase保存、スマホ表示まで確認済み。

## 現在の状態

- `PLAN.md`に初期仕様、データ設計、実装順、検証基準を記載した。
- React + ViteでPWAの初期画面を作成した。
- ダッシュボード、ゲーム絞り込み、単発完了、回数型の`+1／−1`、完了済み折りたたみを実装した。
- タスク追加・編集モーダルを実装した。ゲーム、周期、タスク形式、重要度、所要時間、回数目標、メモを入力できる。
- 追加・編集したタスクは現在の画面状態へ即時反映され、Supabase接続中は永続保存される。
- 「更新までの日数」はユーザー入力から外し、周期から内部で自動計算する方針へ変更した。
- 所要時間は任意入力に変更し、未設定のタスクでは画面に時間を表示しない。
- ゲーム一覧をタスク配列から分離し、新しいゲーム名をタスク保存時にフィルターへ追加するようにした。
- 2週間ごとのタスクでは基準日、期間限定タスクでは開始日・終了日を入力できるようにした。
- ゲーム管理モーダルからゲームの追加、名前変更、休止／再開ができる。
- 休止したゲームはフィルターとダッシュボードから非表示になる。名前変更時は関連タスクも更新される。
- タスク追加・編集モーダルのゲーム欄では、登録済みで有効なゲームを候補ボタンまたは入力補完から選択できる。候補にないゲーム名の自由入力も維持している。
- ゲーム候補はブラウザ標準の狭い前方一致予測を使わず、全角半角・大文字小文字・カタカナ／ひらがな・記号をならして、ゲーム名の一部一致と文字順一致で幅広く検索する。
- 無効化したタスクはゲーム管理画面の「無効化したタスク」欄から確認でき、各タスクを再有効化できる。
- ヘッダーの「☷」から全タスクを一覧で検索・選択できる一括管理モーダルを追加した。表示中の全選択、個別編集、一括無効化、一括削除に対応し、削除時は完了履歴も削除する。
- `@supabase/supabase-js`を追加し、`src/lib/supabase.js`にブラウザ用クライアントの土台を作った。環境変数が未設定でも既存の試作画面は動く。
- `supabase/schema.sql`にゲーム、タスク、期間別進捗、完了履歴のテーブルとRLSポリシーを用意した。
- `SUPABASE_SETUP.md`に、ユーザーがSupabaseプロジェクトを作成してURL・Publishable keyを設定する手順を記載した。
- Supabaseプロジェクトが作成され、初期スキーマの実行に成功した。ローカルの`.env.local`へ接続情報を設定済み（値はこの文書へ記録しない）。
- Supabase Authのログイン／新規登録画面を追加し、セッションをブラウザに保持する設定を有効にした。
- Auth画面にGoogleログインボタンを追加した。メールアドレス／パスワード方式は予備として残している。
- 認証後はゲーム・タスク・期間別進捗をSupabaseから読み込み、タスク操作とゲーム管理の変更をSupabaseへ保存する実装を追加した。
- Supabaseにまだデータがない初回ログイン時は、現在の試作データを初期データとして登録する。
- Supabase URL ConfigurationにローカルのSite URLとRedirect URLを登録し、Google OAuthでログインしてダッシュボード表示まで確認済み。
- `public/manifest.webmanifest`と`public/sw.js`を追加し、PWAの土台を作った。
- 対象ゲームは原神、NTE、崩壊：スターレイル、ドラクエウォーク、ハートピアスローライフ。
- 想定構成はPWA + GitHub Pages + Supabase。
- Supabaseの無料枠、プロジェクト自動停止、バックアップ制限を確認済み。

## 次の作業

1. 公開版で一括管理モーダルを開き、全選択・一括無効化・一括削除を少数の試作タスクで確認する。
2. 既存の試作タスクを一括削除し、必要なタスクを整理して登録する。
3. 単発完了、タスク追加、タスク編集の再読み込み永続性を確認する。
4. 必要に応じてバックアップ／エクスポートと入力・通信エラー表示を強化する。

## 未確認事項

- Supabaseプロジェクトは作成済み。Google OAuthログインまで確認済み。
- GitHubリポジトリ`https://github.com/takubou316/game-daily-manager`を作成し、`main`へ初回プッシュ済み。
- GitHub Pages用のViteサブパス設定と`.github/workflows/deploy.yml`を追加し、`main`へプッシュ済み。
- GitHub Actionsのビルドでは`VITE_SUPABASE_URL`と`VITE_SUPABASE_PUBLISHABLE_KEY`をRepository Secretsから受け取る。
- GitHub Actionsのbuild/deploy成功を確認。公開URLは`https://takubou316.github.io/game-daily-manager/`。
- 公開URLでSupabase接続済みのログイン画面が表示されることを確認。
- Supabase AuthのSite URLを`https://takubou316.github.io/game-daily-manager`へ変更し、公開URLをRedirect URLにも追加済み。ローカルURLは開発用に維持。
- Google Cloud OAuthクライアントの承認済みJavaScript生成元に`https://takubou316.github.io`を追加して保存済み。Supabase callback URIは維持。
- 公開URLでGoogleログインに成功し、Supabaseからタスク一覧が表示されることを確認。
- 本番URLで回数型タスクを操作後、再読み込みしても値が残っていることをユーザーが確認（Supabase永続保存）。
- 公開URLをQRコードでスマホから開き、スマホ実機でも表示できることを確認。
- 初期タスクの具体的なゲーム内名称と優先度は未確定。
- 単発タスクの完了、タスク追加・編集を操作した後のリロード永続性は未確認。
- タスク編集モーダルから無効化・削除ができる。無効化は履歴を残し、削除は確認ダイアログを表示する。
- スマホ確認用に、同じWi-Fiから`http://192.168.1.217:4173/`へ接続できる開発サーバーを起動し、`preview-qr.png`を生成した。ユーザーによるスマホ実機表示確認済み。

## UIメモ

- タスクフォーム上段の3項目は均等幅にする。
- 補足文は短くし、入力欄の幅や折り返しで下段の開始位置がずれないようにする。
