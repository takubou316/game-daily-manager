# 筋トレ統合ロードマップ

作成日: 2026-09-06
作業主体: Claude
関連: [CLAUDE.md](CLAUDE.md)（設計方針の詳細）、training-menuの[CLAUDE.md](../training-menu/CLAUDE.md)

`game-daily-manager`（ゲームタスク管理）と`training-menu`（筋トレメニュー自動生成＋記録PWA）を接続し、
両方を横断して今日やることを一括確認できる全体管理画面を作る。設計の全体像は同ディレクトリの
[CLAUDE.md](CLAUDE.md)を参照。ここではフェーズ分けした実装順序と、各フェーズの検証基準をまとめる。

各フェーズが終わるたびに動作確認→ユーザーへ報告→次のフェーズへ進む。フェーズをまたいで先回りして
実装しない。

## フェーズ0: 土台づくり（設計の文書化）

- [x] `game-daily-manager/CLAUDE.md`を新規作成し、3層アーキテクチャ・データ設計・
      ローカル/クラウド共存方針を記録
- [x] `game-daily-manager/INTEGRATION_ROADMAP.md`（このファイル）を作成
- [x] ルート`CLAUDE.md`のプロジェクト一覧に`game-daily-manager`を追加
- [x] Supabaseスキーマ案（`training_sessions`/`training_session_exercises`/`training_session_sets`）を
      マイグレーションSQLとして用意（[supabase/migrations/20260906_add_training_integration.sql](supabase/migrations/20260906_add_training_integration.sql)）
- [x] `game-daily-manager/HANDOFF.md`に次の作業を追記

検証基準: ドキュメントの内容にユーザーの合意が取れていること（実装はまだしない）。

## フェーズ1: Supabase側の準備

- 上記マイグレーションSQLをSupabase SQL Editorで実行（ユーザー操作）
- RLSが正しく効くか（自分のuser_id以外は読めない）を確認
- 親・子・孫（session→exercise→set）まで実データを1件投入し、複合外部キーが正しく機能するか確認
- （Codexレビュー反映）別のテストアカウントで、他ユーザーの行のSELECT/INSERT/UPDATE/DELETEが
  できないこと、他ユーザーの`session_id`を親として指定した登録が複合外部キー違反で弾かれることを確認

検証基準: 3テーブルが作成され、RLSポリシーが有効になっている。他ユーザー・他人の親IDを使った
不正な紐付けが弾かれることを確認済み。

## フェーズ2: 全体管理画面（トップ）を作る

- `game-daily-manager`に新しいトップ画面ルートを追加し、既存のダッシュボード（tasks一覧・ゲーム管理・
  リソース管理）は1階層下のルートへ移動
- トップ画面の「今日のタスク」一覧: 初期表示は今日期限（`dueDays === 0`）のゲームタスクのみ
- 「すべて見る」切り替えで、未完了タスク全部（今のダッシュボード相当のソート）にも展開できるようにする
- ゲームタスクの完了・+1操作はその場でできるようにする（既存の`TaskRow`コンポーネントを流用）
- この時点では筋トレ欄はダミー表示でよい（実データ接続はフェーズ5）
- PCプレビューで動作確認

検証基準: 全体管理画面から今日期限のタスクが見え、その場で完了・+1操作ができる。「すべて見る」で
既存ダッシュボード相当の一覧に展開できる。既存のゲーム管理・リソース管理機能に影響が出ていない。
画面遷移の追加に伴い、ブラウザの戻る操作・GitHub Pages上でのリロード・既存のブックマークURLが
壊れていないことも確認する。

## フェーズ3: training-menuにログイン選択を追加

- 初回起動時に「Googleでログインしてクラウド同期する」／「ログインせずこの端末だけで使う」の選択画面
- Supabase JS SDKをCDN経由で`<script>`1行追加（npm/Viteは導入しない）。CDN URLは固定バージョン指定に
  し、CDN側の障害時でもアプリ本体（ローカル機能）が起動不能にならないようにする
- 明示的な`syncEnabled`フラグをlocalStorageに持たせる（Supabase SDKがセッションを自動復元しても、
  「ログインせず使う」を選んだ状態なら同期しない）
- Google OAuthのRedirect URLsにtraining-menuの公開URLを登録する
- **認証はgame-daily-managerと共有しない**（2026-09-07決定、別オリジンのため割り切った。CLAUDE.mdの
  「認証は別オリジンで共有しない」参照）。training-menu側で独立してログインする
- 設定画面から後からログイン/ログアウトを切り替えられるようにする
- **最優先の検証**: 「ログインせず使う」を選んだ場合、今までのtraining-menuと完全に同じ挙動になること
  （既存ユーザー体験を壊さないことが最優先）。既存のlocalStorageデータ（設定・記録履歴）が
  ログイン画面追加によって壊れない・消えないことも確認する

検証基準: ログインしない場合の全機能（メニュー生成・記録・グラフ等）が今まで通り動く。ログイン画面の
表示・選択自体が正しく動く（この時点ではログイン後の同期処理はまだ実装しない）。既存データが無傷。

## フェーズ4: training-menu → Supabase同期を実装

- ログイン時、記録確定（`saveSession`）のタイミングでSupabaseへ複製する処理を追加
  （まずローカル保存→オフラインキューに追加→送信）
- オフラインキュー: `pending_sync`をlocalStorageに持ち、起動時・画面復帰時・`online`イベントで再送を試みる
- 送信の重複防止に、セッション・種目・セットそれぞれの階層で端末発行の`local_id`を使う
  （マイグレーションSQL側で各階層にユニーク制約済み）
- キューはユーザー単位で分離する（ログアウト後に別アカウントでログインした場合、前ユーザーの
  未送信キューが新ユーザーの下で誤送信されないようにする）
- ログアウト時・トークン期限切れ時のキューの扱いを決める（送信を止めるか、再ログイン後に送るか）
- 実機（iPhone）で、機内モードにして記録→オンラインに戻して同期されることを確認

検証基準: ログイン状態で記録した内容がSupabaseの3テーブルに正しく反映される。オフライン中に記録しても
データが失われず、オンライン復帰後に自動で追いつく。同じ記録を複数回再送しても重複登録されない。
アカウント切替・ログアウト時に他アカウントのキューが混ざらない。

## フェーズ5: 全体管理画面と筋トレを接続

- 全体管理画面の「今日のタスク」一覧に、`training_sessions`から取得した「今日やったか」を統合表示
  （今日の日付のセッションが1件でもあれば「済み」）
- 筋トレ項目からtraining-menuへの遷移リンクを設置
- 「未」の表示文言に注意する（Codexレビュー指摘）: training-menuでログインしていない・同期を
  有効にしていない場合も見た目上は「未」になってしまう。「未実施」なのか「クラウド未接続で
  分からない」なのかが区別できるよう、表示か説明文で誤解を防ぐ

検証基準: 全体管理画面を開くと、ゲームタスクと並んで筋トレの今日の実施有無が見える。リンクから
training-menuへスムーズに移動できる（移動後はtraining-menu側で別途ログインが必要な場合がある、
「認証は別オリジンで共有しない」を参照）。「未」表示が実施していないのか同期未接続なのか分かる。

## フェーズ6: 実機総合検証・仕上げ

- PC・スマホ両方で、全体管理画面→各アプリの行き来を一通り確認
- `HANDOFF.md`を更新し、完了報告

検証基準: 実機（iPhone）で一通りの操作が迷わずできる（[UI_UX_GUIDELINES.md](../UI_UX_GUIDELINES.md)の
「初見の人でも説明なしで迷わず使えるか」を完了の基準にする）。

## 未決事項（実装しながら判断でよいもの）

- 「今日のトレーニング」は毎日固定で「今日やったか」を表示する想定だが、training-menu側の週間プランで
  休養日にしている日をどう扱うか（気にせず「未」のまま表示するか、休養日は自動的に対象外にするか）は、
  実際の見た目を見てから判断する。
- 記録済みセッションを後から編集・削除した場合、Supabase側にどう反映するか（新規記録のみが同期対象、
  という前提のためスコープ外だったが、実際に使い始めると編集・削除もしたくなる可能性がある）。
- 同期がまだ済んでいない最新の記録が全体管理画面にすぐ反映されないケース（オフライン中の記録など）に、
  最終同期時刻の表示などで気付けるようにするか。
