# 筋トレ統合ロードマップ

作成日: 2026-09-06
作業主体: Claude
関連: [CLAUDE.md](CLAUDE.md)（設計方針の詳細）、training-menuの[CLAUDE.md](../training-menu/CLAUDE.md)

`game-daily-manager`（ゲームタスク管理）と`training-menu`（筋トレメニュー自動生成＋記録PWA）を接続し、
両方を横断して今日やることを一括確認できる全体管理画面を作る。設計の全体像は同ディレクトリの
[CLAUDE.md](CLAUDE.md)を参照。ここではフェーズ分けした実装順序と、各フェーズの検証基準をまとめる。

各フェーズが終わるたびに、動作確認→**Codexへ実装内容をレビューしてもらい大丈夫か相談**→ユーザーへ
報告→次のフェーズへ進む、という順で進める（2026-09-07にユーザー指示で追加した手順。`codex-implementer`
経由でファイル編集はさせずレビューのみ依頼する。依頼方法は[project_codex_cli_windows_sandbox_limitation]
の追記「編集なし・レビュー専用タスクでは、対象ファイルの内容をプロンプトに直接埋め込んで渡す」に従う）。
フェーズをまたいで先回りして実装しない。

## フェーズ0: 土台づくり（設計の文書化）

- [x] `game-daily-manager/CLAUDE.md`を新規作成し、3層アーキテクチャ・データ設計・
      ローカル/クラウド共存方針を記録
- [x] `game-daily-manager/INTEGRATION_ROADMAP.md`（このファイル）を作成
- [x] ルート`CLAUDE.md`のプロジェクト一覧に`game-daily-manager`を追加
- [x] Supabaseスキーマ案（`training_sessions`/`training_session_exercises`/`training_session_sets`）を
      マイグレーションSQLとして用意（[supabase/migrations/20260906_add_training_integration.sql](supabase/migrations/20260906_add_training_integration.sql)）
- [x] `game-daily-manager/HANDOFF.md`に次の作業を追記

検証基準: ドキュメントの内容にユーザーの合意が取れていること（実装はまだしない）。

## フェーズ1: Supabase側の準備（完了、2026-09-07）

- [x] 上記マイグレーションSQLをSupabase SQL Editorで実行
- [x] `training_sessions`/`training_session_exercises`/`training_session_sets`の3テーブルが
      Table Editorで確認できた
- **先送り**: 別テストアカウントでの他ユーザーRLS検証・複合外部キーの不正紐付けテストは、今
  アカウントを1つしか用意していないため、フェーズ4（ログイン機能実装後）に実データ検証と
  まとめて行うことにした（2026-09-07、実務判断）。

検証基準: 3テーブルが作成され、RLSポリシーが有効になっている。他ユーザー・他人の親IDを使った
不正な紐付けの検証はフェーズ4に持ち越し。

## フェーズ2: 全体管理画面（トップ）を作る（完了、2026-09-07）

- [x] `App`内に`view`state('overview'|'gameTasks')を追加し、`OverviewScreen`（新規トップ）と
      既存ダッシュボードを切り替える構成にした（React Routerは元々使っていないアプリなので、
      既存パターンに合わせてstate切り替えにした。URLは変わらない）
- [x] トップ画面の「今日のタスク」一覧: 初期表示は今日期限（`dueDays === 0`）かつ
      `isPendingTaskVisible`（蓄積型はストック1個以上）のゲームタスク
- [x] 「すべて見る」で既存ダッシュボードへ、ダッシュボード側の「←」で全体管理画面へ、
      それぞれ切り替えできるようにした
- [x] ゲームタスクの完了・+1操作はその場でできる（既存の`TaskRow`を流用、`onEdit`は渡さず
      編集ボタン自体を非表示にした。全体管理画面はあくまで確認・完了操作の場という役割分担）
- [x] 筋トレ欄はプレースホルダー表示（実データ接続はフェーズ5）
- [x] PCプレビュー（デモモード）・モバイル幅(375px)で動作確認、既存のリソース管理・ゲーム管理
      モーダルへの影響なしを確認

**Codexレビュー（2026-09-07）で見つかった問題と対応:**
- **修正済み**: `TaskRow`の「編集」ボタンが全体管理画面では押しても何も起きない状態だった
  （編集モーダルがダッシュボード側のJSXにしか描画されないため）。`onEdit`未指定なら編集ボタン
  自体を出さないよう`TaskRow`を修正し、全体管理画面からは`onEdit`を渡さない形にした
- **修正済み**: 新設の`todayTasks`が既存の`activeTasks`と違うフィルタ経路になっており、
  蓄積型でまだ1個も貯まっていないタスクが表示されてしまっていた。`isPendingTaskVisible`を
  掛けて既存ダッシュボードと条件を揃えた
- **修正済み**: ログアウトしても`view`stateが残るため、次回ログイン時に前回見ていた画面から
  始まってしまう。`signOut`で`view`を`'overview'`に戻すよう修正した
- **検証の結果、対応不要と判断**: 「モーダルを開いたまま全体管理画面に戻るとモーダルstateが
  残留するのでは」という懸念があったが、実際に試すと`.modal-backdrop`(z-index:30)がヘッダー
  (z-index:10)を覆うため、モーダルを開いたままヘッダーのボタンを押すこと自体ができない
  （backdropがクリックを吸収する）。既存のUI構造による自然な保護がすでにあるため対応不要
- **既知の制約として認識（今回は対応しない）**: `task.dueDays`はSupabaseからの読み込み時・
  フォーム保存時にのみ計算される「スナップショット」で、画面を開きっぱなしのまま日付をまたいでも
  自動では再計算されない（既存のゲームタスク管理ダッシュボード全体に元々ある仕様）。今回
  `dueDays === 0`を「今日のタスクとして表示するかどうか」の条件に新しく使ったため、この制約が
  今までより可視性に影響しやすくなった。実用上は「アプリを開き直せば直る」程度の影響と考え、
  今回のスコープでは対処しない

検証基準: 全体管理画面から今日期限のタスクが見え、その場で完了・+1操作ができる。「すべて見る」で
既存ダッシュボード相当の一覧に展開できる。既存のゲーム管理・リソース管理機能に影響が出ていない。
編集ボタンが全体管理画面には表示されず、ダッシュボード側では引き続き機能する。

## フェーズ3: training-menuにログイン選択を追加（完了、2026-09-07）

- [x] 初回起動時に「Googleでログインしてクラウド同期する」／「ログインせずこの端末だけで使う」の
      選択モーダル（`training-menu/index.html`の`#sync-choice-modal`）
- [x] Supabase JS SDKをCDN経由で`<script>`1行追加（バージョン固定、npm/Viteは導入しない）
- [x] `training-menu:sync-choice-made`・`training-menu:sync-enabled`をlocalStorageに保持
- [x] Google OAuthのRedirect URLsに`http://localhost:8081/**`・
      `https://takubou316.github.io/training-menu/**`を追加済み（ユーザー操作）
- [x] **認証はgame-daily-managerと共有しない**（別オリジンのため割り切った）
- [x] 記録画面「その他の設定」からログイン/ログアウトを切り替えられるようにした
- [x] 「ログインせず使う」選択時、既存のtraining-menuと完全に同じ挙動であることを確認
- [x] 実機（Chrome、通常のブラウザタブ）でGoogleログイン成功・「クラウド同期: 有効（メール
      アドレス）」表示までユーザー本人が確認

**発見・対応**: 今回の差分をレビューする過程で、以前の作業でコミットし忘れられていた無関係な
バグ修正（体重ラベルの表示不整合）を発見し、別コミット(`70f77cd`)として分離した。

**Codexレビュー（2026-09-07）で見つかった問題と対応:**
- **修正済み**: `signOutFromSync`がSupabaseのsignOut失敗を無視し、失敗時もUI上「ログアウト成功」に
  見えてしまう不整合があった。エラーを見て呼び出し元に返すよう修正
- **修正済み**: 記録画面側の「Googleでログインする」ボタンがエラーを一切表示しないfire-and-forget
  実装だった。モーダル側と同様にエラー表示するよう修正
- **修正済み**: `signInWithGoogleForSync`が`setSyncEnabled(true)`をOAuth呼び出し前に楽観的にセット
  しており、失敗時もフラグだけ残る不整合があった。呼び出し成功後にセットするよう修正
- **修正済み**: `redirectTo`に`window.location.href`（クエリ/ハッシュ込み）をそのまま渡していた。
  Supabaseが付与するトークン用ハッシュと衝突しないよう、オリジン+パスだけに絞った
- **既知の制約として認識（今回は対応しない、詳細はtraining-menu/CLAUDE.mdの「クラウド同期」節）**:
  PWA(ホーム画面追加のstandalone起動)でのOAuthリダイレクト挙動が未検証。フェーズ6の実機総合検証で
  確認する。CDN読み込みが完全に失敗せず「遅い/ハングする」場合の対策も無いが、jsdelivrの可用性を
  踏まえ対応を見送った

検証基準: ログインしない場合の全機能（メニュー生成・記録・グラフ等）が今まで通り動く。ログイン画面の
表示・選択自体が正しく動く（この時点ではログイン後の同期処理はまだ実装しない）。既存データが無傷。
実機（ブラウザタブ）でのGoogleログインが成功する。

## フェーズ4: training-menu → Supabase同期を実装（主要部分完了、2026-09-07）

- [x] `js/workout-log.js`の`finalizeSession`（記録確定）から、ローカル保存の直後に
      `queueSessionForSync`を呼ぶよう配線（`js/sync.js`）
- [x] `training_sessions`/`training_session_exercises`/`training_session_sets`への
      正規化・upsert処理（`syncSessionToSupabase`）。holdBased種目(プランク等)は`hold_sec`、
      cardio種目は`distance_km`/`duration_sec`に正しく振り分ける
- [x] オフラインキュー(`training-menu:pending-sync`)。記録確定時・起動時・ログイン成功時・
      `online`イベントで送信を試み、失敗したエントリはキューに残して以降を打ち切る
      （同じ理由で連鎖的に失敗するのを避けるため）
- [x] 各階層の`local_id`によるupsertで重複防止（同じrecordを2回送っても行が増えないことを
      Browserペインで実地確認済み）
- [x] キューをユーザーIDで分離し、他アカウント宛てのエントリは送信対象から除外することを確認
- [x] **実データ同期をBrowserペインで実地確認**: strength種目(warmup含む複数セット)・
      holdBased種目・cardio種目を含むテストセッションを実際にSupabaseの3テーブルへ書き込み、
      値の正しさ（`hold_sec`/`weight`/`reps`の排他、`distance_km`/`duration_sec`等）を確認
- [x] 「ログインせず使う」状態での記録確定が今まで通り動作し、キューに何も積まれないことを確認
      （既存ユーザー体験への影響なし）
- **見送り（ユーザー承認済み）**: フェーズ1から持ち越していた「2つ目のテストアカウントでの
  RLS検証」は、テスト用メールアドレスがSupabase側で弾かれ想定より手間がかかったため、標準的な
  `auth.uid() = user_id`パターン+複合外部キー制約という設計自体の妥当性（Codexレビュー済み）で
  十分と判断し実施しないことにした
- **未実施（次回、実機で確認）**: 実機（iPhone）で機内モードにして記録→オンラインに戻し、
  自動で同期されることの確認。今回はBrowserペインからのJS直接呼び出しで「失敗時にキューへ残る」
  「オンライン復帰後に再送で成功する」というロジック自体は検証済みだが、実際の`navigator.onLine`
  イベント発火や実機のネットワーク切替を伴う検証はできていない
- **未実施**: ログアウト時・トークン期限切れ時のキュー実地テスト（設計は「ユーザーIDで分離し、
  ログアウトしてもキューはそのまま残る」方針で実装済みだが、明示的なシナリオテストは未実施）

**Codexレビュー（2026-09-07）で見つかった問題と対応:**
- **修正済み（重大）**: `flushSyncQueue`が「開始時に読んだキュー」をそのまま上書き保存していたため、
  実行中(Supabaseへの送信待ちの間)に別の記録が確定してキューに追加されると、その分がまるごと
  消えてしまう競合(read-modify-write)があった。書き戻す直前に最新のキューを再読込し、「今回
  処理し終えたlocalId」だけを差し引くマージ方式に変更して解消
- **修正済み（Claudeの実地テストで追加発見）**: 上記の修正だけでは、実行中に届いた新しいエントリが
  ロックのせいでスキップされたまま送信されず放置される（データは消えないが自動送信されない）
  別の問題が残っていた。ロック中に`flushSyncQueue()`が呼ばれたら`flushRequestedDuringRun`を
  立てておき、今回の実行完了後にもう一度実行し直す仕組みを追加し、Browserペインで実際に
  タイミングを再現して両方とも直っていることを確認済み
- **修正済み**: `finalizeSession`が`queueSessionForSync`の呼び出しをtry/catchで囲んでおらず、
  クラウド同期側の例外（`localStorage`のQuotaExceededError等）が呼び出し元まで伝播しうる状態
  だった。「クラウド側が失敗してもローカル記録には一切影響しない」という設計方針に反するため、
  try/catchで囲んで防御した
- **追加済み**: 恒久的に失敗し続けるエントリ（データ不整合等）が先頭に居座ると、それより後ろの
  正常なエントリが永久に送信されなくなる懸念に対し、同一エントリがMAX_SYNC_ATTEMPTS(5)回失敗
  したら隔離してスキップする仕組みを追加。隔離時は`console.warn`を出す
  （ローカルの記録自体は消えない、クラウド複製だけを諦める）
- **修正済み（軽微）**: `body_weight_kg`取得時の関数存在チェックが`bodyWeightHasStoredValue`にしか
  掛かっておらず`getBodyWeightKg`が漏れていた一貫性の欠如を修正
- **コメントで明示（軽微）**: `syncSessionToSupabase`は複数の独立したHTTPリクエストでありトランザ
  クションではない点（途中で失敗すると一部のテーブルだけ書き込まれた状態になりうるが、`local_id`が
  決定的なため再送すれば自然に辻褄が合う）を、今後のメンテナが誤解しないようコメントで明示
- **既知の制約として認識（今回は対応しない）**: `local_id`が配列インデックス基準（`ex-0`, `set-0`等）
  のため、将来「記録編集で種目やセット数を減らす」機能を追加すると、削除された分の行がSupabase側に
  孤児として残り続ける懸念がある。記録編集機能に着手する際は、決定的なUUIDをrecord自身に持たせる
  かクリーンアップ処理を追加する必要がある。同様に、holdBased判定は`exercises-data.js`の
  `EXERCISES`配列を`exerciseId`で検索する方式のため、将来カスタム種目に対応する場合は
  「マスタに存在しない種目」の扱いを再検討する必要がある

検証基準: ログイン状態で記録した内容がSupabaseの3テーブルに正しく反映される。同じ記録を複数回
再送しても重複登録されない。アカウント切替・ログアウト時に他アカウントのキューが混ざらない。
記録確定を連続して行っても、キュー競合でデータが消えたり送信漏れが起きたりしない。恒久的に
失敗するエントリが後続の送信を妨げない。以上は自動テストで確認済み。実機でのオフライン→
オンライン復帰の確認はフェーズ6と合わせて行う。

## フェーズ5: 全体管理画面と筋トレを接続（完了、2026-09-07）

- [x] `OverviewScreen`の筋トレ欄を、`training_sessions`テーブルを直接クエリした3状態
      （`done`/`not_done`/`unknown`）表示に置き換えた。`unknown`（training_sessionsが0件、
      training-menu側でまだクラウド同期を使ったことがない可能性が高い）と`not_done`（今日以外の
      日付のセッションはある）を別の文言で区別し、「未実施」と「クラウド未接続で分からない」を
      混同しないようにした（前回のCodexレビュー指摘への対応）
- [x] 「開く」リンクを設置（`https://takubou316.github.io/training-menu/`、`target="_blank"`）
- [x] Browserペインから実際にログインし、`unknown`（デモモード）・`done`（training-menu側で
      今日の日付のテストセッションを同期させた状態）・`not_done`（同じテストデータの
      `session_date`を昨日に書き換えた状態）の3状態すべてを実データで確認済み

**Codexレビュー（2026-09-07）で見つかった問題と対応:**
- **修正済み**: `useEffect`の依存配列に`session`オブジェクト全体を使っていた（トークンリフレッシュ
  等で内容が同じでも新しい参照になり、無駄な再フェッチを招く可能性があった）→ `session?.user?.id`
  だけを依存配列に使うよう修正
- **修正済み**: Supabaseクエリの`.then()`に`.catch()`が無く、ネットワーク例外等でPromiseが
  rejectした場合を拾えていなかった → `.catch()`を追加し、失敗時も`unknown`表示のままにする
- **確認済み（対応不要）**: `todayDateKey`（game-daily-manager側）とtraining-menu側の`localDateKey`
  （`js/storage.js`）が同じロジック（ブラウザのローカルタイムゾーンで`getFullYear/getMonth/getDate`）
  であることを実装を突き合わせて確認した。両アプリを同じ端末・同じタイムゾーンで使う前提であれば
  日付判定がずれることはない
- **確認済み（対応不要、既存の設計判断のまま）**: `training_sessions`等のRLSポリシーは
  `for all`（select/insert/update/delete全て）で`auth.uid() = user_id`のみを条件にしており、
  読み取り専用に制限されていない。これは「自分のデータは自分で全操作できる」という既存の
  `games`/`tasks`等と同じ設計判断であり、今回のフェーズでは意図的にselectしか使っていないため
  実害はない

検証基準: 全体管理画面を開くと、ゲームタスクと並んで筋トレの今日の実施有無が見える。リンクから
training-menuへスムーズに移動できる（移動後はtraining-menu側で別途ログインが必要な場合がある、
「認証は別オリジンで共有しない」を参照）。「未」表示が実施していないのか同期未接続なのか分かる。
以上は全て実データで確認済み。

## フェーズ6: 実機総合検証・仕上げ

- PC・スマホ両方で、全体管理画面→各アプリの行き来を一通り確認
- `HANDOFF.md`を更新し、完了報告

検証基準: 実機（iPhone）で一通りの操作が迷わずできる（[UI_UX_GUIDELINES.md](../UI_UX_GUIDELINES.md)の
「初見の人でも説明なしで迷わず使えるか」を完了の基準にする）。

## 未決事項（実装しながら判断でよいもの）

- 「今日のトレーニング」は毎日固定で「今日やったか」を表示する想定だが、training-menu側の週間プランで
  休養日にしている日をどう扱うか（気にせず「未」のまま表示するか、休養日は自動的に対象外にするか）は、
  実際の見た目を見てから判断する。
- 記録済みセッションを後から編集・削除した場合、Supabase側にどう反映するか（新規記録のみが同期対象、
  という前提のためスコープ外だったが、実際に使い始めると編集・削除もしたくなる可能性がある）。
- 同期がまだ済んでいない最新の記録が全体管理画面にすぐ反映されないケース（オフライン中の記録など）に、
  最終同期時刻の表示などで気付けるようにするか。
